# Local (SQLite)
DATABASE_URL="file:./dev.db"

# Produção (Vercel + Turso)
# DATABASE_URL="libsql://<seu-db>.turso.io"
# DATABASE_AUTH_TOKEN="..."

# URL do vdx-render-embed (iframe). Em dev, rode o embed na 5174.
NEXT_PUBLIC_VDX_RENDER_URL="http://localhost:5174"
